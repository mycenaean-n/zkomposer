{
  "name": "circuits",
  "version": "1.0.0",
  "main": "utils/index.ts",
  "license": "MIT",
  "types": "utils/index.ts",
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/jest": "^29.5.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "chai": "4.3.10",
    "circom_tester": "^0.0.20",
    "circomlib": "^2.0.5",
    "mocha": "^10.2.0"
  },
  "scripts": {
    "test": "mocha -r ts-node/register 'test/**/*.ts'",
    "compile": "circom circuits/main.circom --r1cs --sym --wasm -o zk/ ",
    "setup:sample-zkey": "snarkjs groth16 setup zk/main.r1cs  powersOfTau28_hez_final_15.ptau zk/main.zkey",
    "contribute:deterministic": "snarkjs zkey contribute ./zk/main.zkey ./zk/main_final.zkey --name='contributer_1' -v -e='00000'",
    "export:verifier": "snarkjs zkey export solidityverifier zk/main.zkey zk/main_verifier.sol",
    "build:dev": "yarn compile && yarn setup:sample-zkey && yarn contribute:deterministic && yarn export:verifier"
  }
}
